# Development requirements
pytest==7.4.0
pytest-cov==4.1.0
pytest-asyncio==0.21.1
black==23.7.0
flake8==6.1.0
mypy==1.5.1
isort==5.12.0
httpx==0.24.1  # For async HTTP client testing
freezegun==1.2.2  # For time-dependent tests
pytest-env==0.8.2  # For setting environment variables in tests

# Additional test dependencies that are missing
requests==2.31.0  # Required by test_pytest_api.py
sqlalchemy==2.0.23  # Required by test modules importing from src/
fastapi==0.100.0  # Required for API testing
pydantic[email]==2.5.0  # Required for model validation
psycopg2-binary==2.9.9  # Required for database testing
azure-storage-blob==12.19.0  # Required for Azure integration tests
azure-identity==1.15.0  # Required for Azure authentication
python-multipart==0.0.6  # Required for file upload testing
