# HSQ Forms API - Miljökonfiguration för lokal utveckling
# OBS: Kopiera denna fil till .env och fyll i värden för att testa Azure-anslutningar lokalt
# Denna fil ska INTE checkas in i git!

# Miljöinställningar
ENVIRONMENT=development
DEBUG=true
API_TITLE=HSQ Forms API
API_VERSION=0.1.0

# Azure Storage-konfiguration
# Fyll i dessa värden för att testa Azure Storage lokalt
AZURE_STORAGE_ACCOUNT_NAME=       # Exempel: hsqformsdev
AZURE_CLIENT_ID=                  # Exempel: 00000000-0000-0000-0000-000000000000
AZURE_STORAGE_CONTAINER_NAME=form-uploads
AZURE_STORAGE_TEMP_CONTAINER_NAME=temp-uploads
AZURE_STORAGE_ACCOUNT_KEY=        # Valfritt, används endast vid lokal utveckling utan Managed Identity

# Databasanslutning
# Fyll i denna för att testa Azure PostgreSQL lokalt
SQLALCHEMY_DATABASE_URI=          # Exempel: postgresql://hsqadmin:Password123@hsq-forms-pgsql-dev.postgres.database.azure.com:5432/hsq_forms

# För lokal utveckling med Docker
STORAGE_TYPE=local                # Ändra till 'azure' för att testa Azure Storage
LOCAL_STORAGE_PATH=./uploads
TEMP_UPLOAD_DIR=./uploads/temp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=hsq_forms
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# CORS inställningar
ALLOW_CORS=true
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Formuläruppladning
MAX_FORM_SIZE_MB=10
ALLOWED_EXTENSIONS=.pdf,.jpg,.jpeg,.png,.doc,.docx,.xls,.xlsx

# Webhooks (för integration med andra system)
WEBHOOKS_ENABLED=false
WEBHOOK_URL=
WEBHOOK_SECRET=

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_RATE=100/minute
REDIS_URL=  # Valfritt, för Redis rate limiting-backend
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000
