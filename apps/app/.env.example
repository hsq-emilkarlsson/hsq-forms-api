# Environment variables for development
ENVIRONMENT=development

# PostgreSQL Database Configuration
# For local development with docker-compose
DATABASE_URL=postgresql://formuser:formpassword@localhost:5432/formdb

# Individual database connection parameters (alternative to DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=formdb
DB_USER=formuser
DB_PASSWORD=formpassword

# For production, use secure values
# DATABASE_URL=postgresql://username:password@hostname:port/database

# CORS Configuration
# Lägg till din SWA-URL nedan när den är skapad, t.ex.:
# ALLOWED_ORIGINS=https://hsq-feedback-swa.azurestaticapps.net,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
